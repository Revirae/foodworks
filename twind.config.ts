import { defineConfig } from "$fresh/plugins/twind.ts";

export default defineConfig({
  theme: {
    extend: {
      colors: {
        primary: "#3b82f6",
        secondary: "#8b5cf6",
      },
    },
  },
});

